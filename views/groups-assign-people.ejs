<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h1><%=groups.name_of_group%></h1>
    <form action="/groups/<%=groups.id%>" method="post">
      <select name="contact_id">
        <%for(var i in contacts){%>
        <option value="<%=contacts[i].id%>"><%=contacts[i].name%></option>
        <%}%>
      </select><br>
      <input type="submit" name="submit" value="submit">
    </form>

  </body>
</html>
