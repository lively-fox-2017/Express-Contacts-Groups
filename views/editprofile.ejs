<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Add Profile</title>
  </head>
  <body>
    <table>
      <thead>
        <td>

        <th>username</th>
        <th>Password</th>
        </td>
      </thead>
      <tbody>
        <td>
        <form action="/profile/edit/<%=dataProfile.id%>" method="post">
          <td><input type="text" name="username" value="<%=dataProfile.username%>"placeholder="username"></td>
          <td><input type="text" name="password" value="<%=dataProfile.password%>"placeholder="password"></td>
          <select name="Contactsid">
            <%dataContact.forEach(data=>{%>
              <option value="<%=data.id%>"><%=data.name%></option>
            <%})%>
          </select>
          <td><input type="submit" value="submit"></td>
        </form>
        </td>
      </tbody>
    </table>
  </body>
</html>
